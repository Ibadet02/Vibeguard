# dependencies
node_modules/

# netlify CLI local state
.netlify/

# secrets / provider keys (never commit; .env.example is the template)
.env
.env.local
**/.env
**/.env.local

# local scanner output
vibeguard-report.md
report.md

# editor / OS
.DS_Store
*.log

# local-only agent settings
.claude/settings.local.json

# batch research output: names real third-party repos alongside their findings,
# so it stays local. The scripts that produce it are committed, the data is not.
vibeguard-scanner/research/
